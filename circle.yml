machine:
  services:
    - docker

dependencies:
  override:
    - make install-ci

test:
  override:
    - make test-ci

deployment:
  feature-branch:
    branch: /^((?!master).)*$/
    commands:
      - make check-version